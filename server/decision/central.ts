import {independentAddressInvestigations,type AddressInput} from '../checkout/address';
export type Decision={decision:'APPROVE'|'HUMAN_REVIEW'|'BLOCK';reasonCode:string;confidence:number;policyVersion:string;aiResults:any[];createdAt:string};
export function centralDecision(address:AddressInput):Decision{const r=independentAddressInvestigations(address);const results=[r.ai1,r.ai2];if(r.ai2.classification==='PROHIBITED')return {decision:'BLOCK',reasonCode:'PROHIBITED_DESTINATION',confidence:Math.min(r.ai1.confidence,r.ai2.confidence),policyVersion:'2026-09-05',aiResults:results,createdAt:new Date().toISOString()};if(r.conflict||r.ai1.needsHumanReview||r.ai2.needsHumanReview)return {decision:'HUMAN_REVIEW',reasonCode:'ADDRESS_REQUIRES_REVIEW',confidence:Math.min(r.ai1.confidence,r.ai2.confidence),policyVersion:'2026-09-05',aiResults:results,createdAt:new Date().toISOString()};return {decision:'APPROVE',reasonCode:'ALL_CHECKS_PASSED',confidence:Math.min(r.ai1.confidence,r.ai2.confidence),policyVersion:'2026-09-05',aiResults:results,createdAt:new Date().toISOString()};}
