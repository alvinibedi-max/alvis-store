import {randomBytes} from 'node:crypto';import {redisSetJson,redisGetJson} from '../db/redis';
export type Shipment={id:string;orderId:string;method:string;price:number;estimatedDelivery?:string;trackingNumber?:string;carrier?:string;status:'PENDING'|'SHIPPED'|'IN_TRANSIT'|'DELIVERED'|'RETURNED';createdAt:string;updatedAt:string};
export async function createShipment(input:Omit<Shipment,'id'|'status'|'createdAt'|'updatedAt'>){const id=`shp_${randomBytes(10).toString('hex')}`;const now=new Date().toISOString();const s={...input,id,status:'PENDING' as const,createdAt:now,updatedAt:now};await redisSetJson(`shipment:${id}`,s);return s;}
export async function updateShipment(id:string,input:Partial<Pick<Shipment,'trackingNumber'|'carrier'|'status'|'estimatedDelivery'>>){const s=await redisGetJson<Shipment>(`shipment:${id}`);if(!s)throw new Error('Shipment not found.');const updated={...s,...input,updatedAt:new Date().toISOString()};await redisSetJson(`shipment:${id}`,updated);return updated;}
